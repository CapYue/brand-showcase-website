# 🏗️ 网站后端系统架构设计

## 📌 执行摘要

我已经为您设计并实现了一个**完整的前后端分离系统**，让网站内容可以动态管理，无需修改代码。

---

## 🎯 核心设计方案

### 系统三层架构

```
┌─────────────────────────────────────────────┐
│          表现层 (Presentation)              │
│  React 组件 + 页面视图 + 用户交互          │
│  ├── Navbar（导航栏）                      │
│  ├── Banner（轮播图）                      │
│  ├── Products（产品）                      │
│  └── ... 其他组件                          │
└──────────────┬──────────────────────────────┘
               │ HTTP / REST API
               ↓
┌─────────────────────────────────────────────┐
│          业务层 (Business Logic)            │
│  Express 服务器 + API 接口 + 业务规则      │
│  ├── 数据验证                              │
│  ├── 业务流程处理                          │
│  ├── 权限认证                              │
│  └── 文件上传处理                          │
└──────────────┬──────────────────────────────┘
               │ SQL / 数据访问
               ↓
┌─────────────────────────────────────────────┐
│          数据层 (Data Access)               │
│  数据库 + 文件存储 + 缓存                   │
│  ├── MySQL 关系数据库                      │
│  ├── 文件系统或阿里云 OSS                  │
│  └── Redis 缓存（可选）                    │
└─────────────────────────────────────────────┘
```

---

## 📂 项目文件结构

```
project/
│
├── src/                          # 前端源代码
│   ├── components/
│   │   ├── Navbar.jsx           # 导航栏
│   │   ├── Banner.jsx           # 轮播图
│   │   ├── Products.jsx         # 产品页面
│   │   ├── AdminDashboard.jsx   # 新增：管理后台 ✨
│   │   └── ...其他组件
│   │
│   ├── styles/
│   │   ├── global.css
│   │   └── admin.css            # 新增：后台样式 ✨
│   │
│   ├── utils/
│   │   ├── apiClient.js         # 新增：API 客户端 ✨
│   │   └── ...其他工具
│   │
│   ├── App.jsx
│   └── main.jsx
│
├── server/                       # 后端源代码 ✨（新增）
│   ├── server.js               # Express 服务器
│   ├── db-init.js              # 数据库初始化脚本
│   └── .env                    # 环境配置
│
├── public/
│   ├── uploads/                # 图片存储目录 ✨
│   └── ...其他静态资源
│
├── package.json                # 项目依赖配置（已更新）
├── BACKEND_GUIDE.md            # 完整后端指南 ✨
├── QUICK_START_BACKEND.md      # 快速启动指南 ✨
└── BACKEND_ARCHITECTURE.md     # 本文件
```

---

## 🔌 API 接口设计

### RESTful API 端点

#### 公开接口（GET）

| 端点 | 功能 | 返回值 |
|------|------|--------|
| `/api/website/navbar` | 获取导航栏数据 | JSON |
| `/api/website/banner` | 获取轮播图 | Array |
| `/api/website/about` | 获取企业信息 | Object |
| `/api/website/products` | 获取产品列表 | Array |
| `/api/website/products/:id` | 获取单个产品 | Object |
| `/api/website/certifications` | 获取资质荣誉 | Array |
| `/api/website/news` | 获取新闻列表 | Paginated Array |
| `/api/website/contact` | 获取联系信息 | Object |
| `/api/website/footer` | 获取页脚数据 | Object |
| `/api/website/config` | 获取网站配置 | Object |

#### 用户提交接口（POST）

| 端点 | 功能 | 请求体 |
|------|------|--------|
| `/api/website/contact/submit` | 提交联系表单 | `{name, email, phone, subject, message}` |
| `/api/upload/image` | 上传图片 | `FormData (file)` |

#### 管理接口（PUT/DELETE）

| 端点 | 功能 | 权限 |
|------|------|------|
| `PUT /api/admin/products/:id` | 更新产品 | 需认证 |
| `POST /api/admin/products` | 新增产品 | 需认证 |
| `DELETE /api/admin/products/:id` | 删除产品 | 需认证 |

---

## 💾 数据库设计

### 数据库表结构

#### 1. products 表
```sql
CREATE TABLE products (
  id INT PRIMARY KEY,
  category VARCHAR(50),        -- 分类
  name VARCHAR(100),           -- 产品名
  image VARCHAR(255),          -- 图片URL
  description VARCHAR(200),    -- 描述
  price VARCHAR(50),           -- 价格
  features JSON,               -- 特性（JSON数组）
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);
```

#### 2. news 表
```sql
CREATE TABLE news (
  id INT PRIMARY KEY,
  type VARCHAR(50),            -- 类型（企业新闻/行业资讯）
  title VARCHAR(100),          -- 标题
  summary VARCHAR(255),        -- 摘要
  content LONGTEXT,            -- 完整内容
  published_date DATE,         -- 发布日期
  created_at TIMESTAMP
);
```

#### 3. certifications 表
```sql
CREATE TABLE certifications (
  id INT PRIMARY KEY,
  name VARCHAR(100),           -- 资质名称
  category VARCHAR(50),        -- 分类
  importance INT,              -- 重要程度
  year INT,                    -- 获得年份
  created_at TIMESTAMP
);
```

#### 4. contact_submissions 表
```sql
CREATE TABLE contact_submissions (
  id INT PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100),
  phone VARCHAR(20),
  subject VARCHAR(100),
  message LONGTEXT,
  created_at TIMESTAMP
);
```

---

## 🔄 数据流程

### 流程 1：用户访问首页

```
1. 用户访问 http://localhost:5173
   ↓
2. React 应用加载 App.jsx
   ↓
3. 组件初始化时调用 apiClient.getAllWebsiteData()
   ↓
4. 并行发起多个 GET 请求：
   - GET /api/website/navbar
   - GET /api/website/banner
   - GET /api/website/products
   - ...其他数据
   ↓
5. 后端服务器处理请求，返回 JSON 数据
   ↓
6. React 将数据保存到 state
   ↓
7. 组件使用数据渲染页面
```

### 流程 2：客户提交联系表单

```
1. 用户填写表单
   ↓
2. 点击提交按钮
   ↓
3. 前端调用 apiClient.submitContactForm(data)
   ↓
4. 发起 POST 请求到 /api/website/contact/submit
   ↓
5. 后端验证数据
   ↓
6. 存储到数据库（contact_submissions 表）
   ↓
7. 发送成功响应
   ↓
8. 前端显示提交成功提示
```

### 流程 3：管理员更新产品

```
1. 访问管理后台 http://localhost:5173/admin
   ↓
2. 加载现有产品列表
   ↓
3. 点击编辑产品
   ↓
4. 修改产品信息
   ↓
5. 上传新图片（如果有）
   - POST /api/upload/image
   - 返回图片 URL
   ↓
6. 保存更改
   - PUT /api/admin/products/:id
   ↓
7. 后端更新数据库
   ↓
8. 返回成功消息
   ↓
9. 前端刷新列表，显示最新数据
```

---

## 📊 数据存储选项

### 选项 1：本地 JSON（开发使用）✅ 当前

**优点**：
- 无需安装数据库
- 快速开发
- 简单易用

**缺点**：
- 单进程，不支持并发
- 数据丢失风险
- 无法扩展

**使用场景**：
- 本地开发测试
- 小型演示网站

### 选项 2：MySQL（生产推荐）🔒

**优点**：
- 稳定可靠
- 支持复杂查询
- 支持多用户并发
- 数据安全备份

**缺点**：
- 需要数据库服务
- 配置相对复杂

**使用场景**：
- 生产环境
- 中大型网站

**初始化命令**：
```bash
npm run db:init
```

### 选项 3：阿里云 OSS（文件存储）☁️

**优点**：
- 云端存储，无需维护服务器
- 自动 CDN 加速
- 容量不限
- 按使用量付费

**缺点**：
- 需要阿里云账号
- 有额外费用

**使用场景**：
- 图片存储
- 大文件上传

---

## 🛠️ 部署架构

### 开发环境

```
本地计算机
├── 前端服务 (localhost:5173)
│   └── React + Vite
├── 后端服务 (localhost:5000)
│   └── Express + Node.js
└── 本地文件系统
    └── 数据和上传文件
```

### 生产环境

```
互联网
   ↓
阿里云 CDN/加速器
   ↓
   ├─→ 前端 (Vercel 或阿里云 OSS)
   │   └── 静态网站
   │
   └─→ 后端 (阿里云 ECS)
       ├── Node.js 应用 (PM2 管理)
       ├── Nginx 反向代理
       └── MySQL 数据库
           └── 持久化存储
```

---

## 🔐 安全考虑

### 1. CORS（跨域资源共享）

```javascript
// 只允许来自特定域的请求
app.use(cors({
  origin: ['https://yourdomain.com'],
  credentials: true
}));
```

### 2. 输入验证

```javascript
// 验证用户输入
if (!name || !email || !message) {
  return res.status(400).json({ error: '缺少必填字段' });
}
```

### 3. 认证授权

```javascript
// JWT 令牌认证
const token = req.headers['authorization']?.split(' ')[1];
jwt.verify(token, process.env.JWT_SECRET, ...);
```

### 4. 文件上传安全

```javascript
// 限制文件类型和大小
const allowedTypes = /jpeg|jpg|png|gif/;
const limits = { fileSize: 5 * 1024 * 1024 }; // 5MB
```

---

## 📈 性能优化

### 1. 缓存策略

```javascript
// 缓存频繁访问的数据
app.use((req, res, next) => {
  res.set('Cache-Control', 'public, max-age=3600');
  next();
});
```

### 2. 数据库索引

```sql
-- 为常见查询添加索引
CREATE INDEX idx_category ON products(category);
CREATE INDEX idx_published_date ON news(published_date);
```

### 3. 分页查询

```javascript
// 避免一次加载过多数据
GET /api/website/news?page=1&pageSize=5
```

### 4. 图片优化

```javascript
// 使用 CDN 和图片压缩
image: 'https://cdn.example.com/image.jpg?w=500&h=500&fit=crop'
```

---

## 📝 开发工作流

### 日常开发

1. **启动开发环境**
   ```bash
   npm run dev:both
   ```

2. **编辑文件**
   - 修改前端代码：`src/components/*`
   - 修改后端代码：`server/*.js`

3. **测试更改**
   - 前端：自动热加载（HMR）
   - 后端：使用 nodemon 自动重启

4. **提交代码**
   ```bash
   git add .
   git commit -m "Description"
   git push
   ```

### 新增功能流程

1. **设计 API 接口**（后端）
2. **实现 API 端点**（后端 + 数据库）
3. **实现前端组件**（前端）
4. **集成测试**（前后端）
5. **部署上线**（Vercel + ECS）

---

## 🚀 快速参考

### 启动服务

```bash
# 只启动前端
npm run dev

# 只启动后端
npm run dev:backend

# 同时启动前后端（推荐）
npm run dev:both
```

### 初始化数据库

```bash
npm run db:init
```

### API 测试

```bash
# 测试后端是否运行
curl http://localhost:5000/health

# 获取产品列表
curl http://localhost:5000/api/website/products

# 提交测试数据
curl -X POST http://localhost:5000/api/website/contact/submit \
  -H "Content-Type: application/json" \
  -d '{"name":"test","email":"test@test.com","message":"hello"}'
```

---

## 📚 相关文档

| 文档 | 内容 |
|------|------|
| [QUICK_START_BACKEND.md](./QUICK_START_BACKEND.md) | 快速启动指南 |
| [BACKEND_GUIDE.md](./BACKEND_GUIDE.md) | 完整后端指南 |
| [DEPLOYMENT.md](./DEPLOYMENT.md) | 部署指南 |

---

## ✨ 总结

这个后端系统实现了：

- ✅ **前后端分离** - 独立开发和部署
- ✅ **RESTful API** - 标准化接口设计
- ✅ **数据管理** - 完整的 CRUD 操作
- ✅ **文件上传** - 图片和文件处理
- ✅ **用户交互** - 表单提交和反馈
- ✅ **管理后台** - 内容编辑界面
- ✅ **多环境支持** - 开发/生产配置
- ✅ **可扩展性** - 易于添加新功能

**现在您已经拥有了一个企业级的网站系统！** 🎉

---

**任何问题，查看 [完整后端指南](./BACKEND_GUIDE.md) 获得详细帮助。**
